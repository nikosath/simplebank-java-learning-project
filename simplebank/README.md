# SimpleBank

Starter Java project for the SimpleBank learning path.
Feel free to use your preferred Java version and testing framework, but the project is set up with Java 17 and JUnit 5 by default.

## Layout

- `src/main/java` for application code
- `src/test/java` for tests
- `sql/` for database scripts used in later versions
